@{
    ViewData["Title"] = "Add New Item";
}

@model ADET_FINAL_PROJECT.Models.AddItemViewModel

<div class="container pt-3 pb-4 justify-content-center" style="width: 100%;">
    <h2 class="mb-4" style="font-family: 'Caveat', cursive;">Add New Item</h2>

    <div class="container pt-3 pb-4" style="max-width: 800px">
        <div class="d-flex justify-content-end mb-3">
            <a asp-controller="Item" asp-action="Add" class="btn btn-danger rounded-pill fw-bold">Cancel Add Item</a>
        </div>
        <form method="post" class="text-center fw-bold">
            <div class="mb-3">
                <label class="form-label fw-semibold">Name</label>
                <input class="form-control rounded-pill text-center" asp-for="Name" required />
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold">Category</label>
                <select class="form-select rounded-pill text-center" required asp-for="Category">
                    <option value="" selected disabled>Select Category</option>
                    <option value="Flower">Flower</option>
                    <option value="Alcohol">Alcohol</option>
                    <option value="Flower & Alcohol">Flower & Alcohol</option>
                </select>
            </div>

            <div class="mb-3">
                <label class="form-label fw-semibold">Description</label>
                <input class="form-control rounded-pill text-center" asp-for="Description" required />
            </div>

            <div class="mb-4">
                <label class="form-label fw-semibold">Quantity</label>
                <input class="form-control rounded-pill text-center" type="number" asp-for="Quantity" required />
            </div>

            <div class="d-flex gap-3 mb-3">
                <button type="submit" class="btn btn-success flex-fill rounded-pill fw-bold">Confirm</button>
                <a asp-controller="Item" asp-action="Inventory" class="btn btn-danger flex-fill rounded-pill fw-bold">Cancel</a>
            </div>
        </form>
    </div>

</div>
</div>